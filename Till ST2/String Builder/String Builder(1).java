/*
  String Builder 

    - it is similar to string buffer
    - only diff is it is not synchronized
    - it is not thread safe
    - mutable string objexts 
    - use this if synchronization is not required
    - java.lang package 
    - use this if we want to save the thread or give multiple threads the access.


    String Builder constrcutors 
        StringBuilder() - created space for 16 character i.e capacity
        StringBuilder(str)
        StringBuilder(String str)

    It has all the methods similar to 'StringBuffer'

        .append()
        .insert()
        .replace()
        .reverse()
        


 */
public class StringBuilderIntro {
    
}
